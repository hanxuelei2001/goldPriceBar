# Android 版本号唯一来源。
#
# app/build.gradle.kts（写入 APK 清单）、scripts/build-android.sh 与
# .github/workflows/*.yml（决定产物文件名）都读这一份，避免包内版本与文件名不一致。
#
# 发布新版本时与下面两处一起改：
#   - scripts/build-dmg.sh         的 VERSION
#   - Windows/Directory.Build.props 的 <Version>（以及 scripts/build-windows.ps1 的 ZIP 名）
versionCode=3
versionName=1.0.3
